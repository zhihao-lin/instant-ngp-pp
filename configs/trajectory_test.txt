root_dir = ./tnt/Playground_153_seg_woHouse
dataset_name = tnt
exp_name = trajectory_test_1
batch_size = 2048
scale = 8.0
num_epochs = 20

### render a camera path(through interpolation between poses)
render_path = True 

### render camera poses from training dataset
render_train = False

render_rgb = True
render_depth = False

### render derived normal or not
render_normal = False
render_normal_up = False

### render semantic labels or not, set to False if no g.t. semantic labels
render_semantic = False

### appearance embeddings
embed_a = True
embed_a_len = 6

random_bg = True
### optimize_ext = True