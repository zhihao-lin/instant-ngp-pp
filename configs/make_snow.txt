root_dir = ./tnt/Playground
dataset_name = tnt
exp_name = Playground_snow_scatter_style
batch_size = 2048
scale = 8.0
num_epochs = 30

### render a camera path(through interpolation between poses)
render_path = True 

### render camera poses from training dataset
render_train = True

render_rgb = True
render_depth = True

### appearance embeddings
embed_a = True
embed_a_len = 6

random_bg = True
### optimize_ext = True