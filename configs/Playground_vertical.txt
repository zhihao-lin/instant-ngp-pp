root_dir = ./tnt/Playground
dataset_name = tnt
exp_name = Playground_vertical
batch_size = 8192
scale = 8.0
num_epochs = 20

### render a camera path(through interpolation between poses)
render_path = True 

### render camera poses from training dataset
render_train = False

render_rgb = True
render_depth = True

### render derived normal or not
render_normal = True

### appearance embeddings
embed_a = True
embed_a_len = 8

random_bg = True
### optimize_ext = True